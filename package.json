{
  "name": "agent-market",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n node,api,indexer,web -c gray,blue,green,yellow \"pnpm --filter @agent-market/contracts node\" \"DOTENV_CONFIG_PATH=../../.env pnpm --filter @agent-market/api dev\" \"DOTENV_CONFIG_PATH=../../.env pnpm --filter @agent-market/indexer dev\" \"VITE_API_BASE=http://localhost:3000 pnpm --filter @agent-market/web dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
