{
  "name": "agent-market",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,indexer,worker,web -c blue,green,magenta,yellow \"pnpm --filter @agent-market/api dev\" \"pnpm --filter @agent-market/indexer dev\" \"pnpm --filter @agent-market/jury-worker dev\" \"pnpm --filter @agent-market/web dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
